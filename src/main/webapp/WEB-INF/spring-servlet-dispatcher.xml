<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context 
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc 
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Escaneo de controladores -->
    <context:component-scan base-package="es.ticketcenture.controller" />

    <!-- Habilitar anotaciones de Spring MVC -->
    <mvc:annotation-driven />

    <!-- Resolver de vistas JSP -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>
    
    <!-- Cargar mensajes desde mensajes.properties --> 
    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
   	 	<property name="basename" value="classpath:mensajes" />
    	<property name="defaultEncoding" value="UTF-8" />
    </bean>
    
    <!-- Registrar MessageManager y pasarle el messageSource -->
	<bean class="es.ticketcenture.utilities.MessageManager">
    	<property name="messageSource" ref="messageSource" />
	</bean>
	<!-- Necesario para procesar formularios con archivos -->
	<bean id="multipartResolver"
      class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
    <property name="maxUploadSize" value="5242880" /> <!-- 5MB -->
	</bean>



</beans>
